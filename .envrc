# Pin every shell in this repo to the chaincheck GCP/Firebase project.
# Several projects on this machine target different GCP projects; never rely
# on the machine-global active gcloud config.
export CLOUDSDK_ACTIVE_CONFIG_NAME=chaincheck
export CLOUDSDK_CORE_PROJECT=chaincheck-app
export GOOGLE_CLOUD_PROJECT=chaincheck-app
